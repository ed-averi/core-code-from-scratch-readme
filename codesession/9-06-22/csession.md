```ts
// Interfaz sive para definir lo que debe cumplir una clase
export default interface Audible {
  volumen: number;
  play(sojng: string): void;
}

//Clase abstracta => Permite compartir comportamientos
//Diferencia entre interfaz y clase abstracta
```
